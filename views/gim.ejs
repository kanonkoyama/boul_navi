<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>boul_navi</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <%- include('header'); %>
    <div class="main_wrapper">
      <div class="container">
        <main>
          <h2><%= gim.name %></h2>
          <h2>こんなジム！</h2>
          <%= gim.content %>
          <h2>このジムの口コミ</h2>
          <a href="/review/<%= gim[0]["id"] %>">口コミを書く</a>
          <ul>
          <% reviews.forEach((review) => { %>
            <li>
              <%= review.name %>
              <%= console.log(review.name) %>
              <br>
              <%= review.content %>
            </li>  
            <% }); %>
          </ul>  
          <%= console.log(gim.name)%>
          <table>
            <caption>このジムの詳細</caption>
              <tbody>
                <tr>
                  <td>所在地</td>
                  <td><%= gim[0]["address"] %></td>
                  <% console.log("hello") %>
                </tr>
                <tr>
                  <td>電話番号</td>
                  <td><%= gim[0]["tell"] %></td>
                </tr>
                <tr>
                  <td>営業時間</td>
                  <td><%= gim[0]["time"] %></td>
                </tr>
                <tr>
                  <td>定休日</td>
                  <td><%= gim[0]["regular_holiday"] %></td>
                </tr>
                <tr>
                  <td>初回登録料</td>
                  <td><%= gim[0]["initial_registration"] %></td>
                </tr>
                <tr>
                  <td>利用料</td>
                  <td><%= gim[0]["price"] %></td>
                </tr>
                <tr>
                  <td>ホームページ</td>
                  <td><%= gim[0]["website"] %></td>
                </tr> 
              </tbody>
          </table>
        </main>
      </div>
    </div>
    <footer>
    </footer>
  </body>
</html>